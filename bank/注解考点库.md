# 注解考点库（共享）

> 每学到一个注解，都要问"删了会怎样 / 为什么需要 / 替代方案 / Spring 什么时候处理"。

## 启动 / 配置

- @SpringBootApplication
  - 拆开是哪三个？
  - 删了会怎样？
  - 为什么启动类要放根包？
- @SpringBootConfiguration
- @EnableAutoConfiguration
- @ComponentScan
- @Configuration
  - 和 @Component 区别？
  - proxyBeanMethods = true 干嘛？
- @Bean
  - 和 @Component 区别？
  - 名字怎么定？
- @ConfigurationProperties
  - 和 @Value 区别？
  - 怎么激活？

## Web 层

- @RestController
  - = @Controller + @ResponseBody？
- @Controller
- @RequestMapping
- @GetMapping / @PostMapping / @PutMapping / @DeleteMapping
- @RequestBody
  - 什么时候必须用？GET 能用吗？
- @RequestParam
- @PathVariable
- @RequestHeader
- @RestControllerAdvice
  - 和 @ControllerAdvice 区别？
- @ExceptionHandler
- @InitBinder
- @CrossOrigin

## 依赖注入

- @Autowired
  - 字段注入 vs 构造器注入？
  - byType vs byName？
- @Resource
  - 和 @Autowired 区别？
- @Qualifier
- @Primary
- @Component / @Service / @Repository
  - 本质区别？
- @Lazy
- @Scope

## 持久层

- @TableName
- @TableId
  - IdType.AUTO / INPUT / ASSIGN_ID 区别？
- @TableField
  - fill 三种取值？
- @TableLogic
- @Version
- @Mapper
  - 和 @MapperScan 区别？
- @Select / @Insert / @Update / @Delete

## 校验

- @Valid
- @Validated
  - 区别？
- @NotNull / @NotBlank / @NotEmpty
  - 分别用在什么类型？
- @Size / @Min / @Max
- @Email / @Pattern
- @Positive / @Negative

## 事务

- @Transactional
  - 加在什么上？
  - rollbackFor 默认值？
  - 失效场景？
- @EnableTransactionManagement（Spring Boot 自动开）

## AOP / 其他

- @Aspect
- @Pointcut
- @Around / @Before / @After
- @Async
  - 为什么要 @EnableAsync？
- @Scheduled
  - 为什么要 @EnableScheduling？
- @Slf4j（Lombok）
- @Data / @Getter / @Setter / @Builder
  - @Data 有什么坑？

## 每个注解必问的四个问题

1. 删掉会发生什么？
2. 为什么必须用它？
3. 有没有替代方案？
4. Spring 什么时候处理它？
