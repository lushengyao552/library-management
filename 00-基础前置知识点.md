# 基础前置知识点：Spring 容器 / Bean / 依赖注入 / 启动流程

> 本文件是训练营的"补课区"。凡是在做题时发现"这个名词见过但讲不清楚"的基础概念，都会整理到这里。
> 阅读方式：按顺序读，每节末尾有"自测"，能用自己的话讲出来才算过。
> 当前收录：① Spring 容器与 Bean ② IOC/DI ③ @Component 与 @Bean ④ @Configuration 与 CGLIB 代理 ⑤ @Bean 返回接口对象（ApplicationRunner）⑥ Lambda 与匿名内部类 ⑦ Spring Boot 启动流程

---

## 一、Spring 容器与 Bean（一切的起点）

### 1.1 没有 Spring 之前，对象怎么来？

```java
public class BorrowService {
    private UserService userService = new UserService();   // 自己 new
    private BookService bookService = new BookService();   // 自己 new
}
```

问题：
- 每个地方都要 `new`，代码到处重复；
- 想换一个实现（比如测试时换 mock），要改所有 new 的地方；
- 对象的创建、销毁、单例与否，全靠程序员自己管。

### 1.2 Spring 的做法：容器替你管

Spring 启动时创建一个**容器（IOC 容器）**，里面放满了**Bean**。

- **Bean**：被 Spring 容器创建并管理的对象（一个类的实例）。
- **容器**：装这些 Bean 的"仓库"，负责创建、装配、销毁。

```text
你的代码不再 new，而是告诉容器"我要一个 UserService"
容器从仓库里拿出来给你（或注入给你）
```

### 1.3 为什么叫 IOC（控制反转）

- **正转**：你自己控制"什么时候 new、new 几个"。
- **反转**：控制权交还给容器，你只声明"我需要什么"。

> 面试一句话：IOC 就是"对象创建的控制权从程序员手里反转给 Spring 容器"。

### 1.4 自测

1. 用自己的话解释：什么是 Bean？什么是容器？
2. 为什么要反转控制权？不反转会有什么麻烦？

---

## 二、依赖注入 DI（IOC 的落地方式）

### 2.1 概念

IOC 是思想，DI（Dependency Injection，依赖注入）是实现手段：
**容器把依赖的对象"塞"给你，而不是你自己去 new。**

### 2.2 三种注入方式对比

| 方式 | 写法 | 是否推荐 | 原因 |
|---|---|---|---|
| 构造器注入 | `public BorrowService(UserService us) { this.us = us; }` | ✅ 最推荐 | 依赖不可变、必填、容易测试 |
| Setter 注入 | `public void setUs(UserService us)` | ⚠️ 可选依赖 | 依赖可改，但可能忘记 set |
| 字段注入 | `@Autowired private UserService us;` | ⚠️ 少用 | 写起来短，但隐藏依赖、难测试 |

Spring Boot 官方推荐**构造器注入**。Lombok 的 `@RequiredArgsConstructor` 就是帮你自动生成构造器，所以你会看到项目里 `private final UserService userService;` 就能注入——`final` 字段会进构造器，Lombok 生成构造器，Spring 用构造器注入。

### 2.3 方法参数注入（本训练营大量使用）

```java
@Bean
public ApplicationRunner printAppInfoRunner(AppInfo appInfo) {
    // 注意：方法参数 AppInfo appInfo 不是自己 new 的
    // Spring 调用这个方法时，自动从容器里找 AppInfo 塞进来
    return args -> { ... };
}
```

> 关键：**Spring 在调用 @Bean 方法时，会先解析方法参数，从容器里查有没有对应的 Bean，有就传进来。** 这就是"方法参数注入"。

### 2.4 自测

1. 三种注入方式分别怎么写？推荐哪个？
2. 为什么 `@RequiredArgsConstructor` + `private final` 就能注入？
3. `@Bean` 方法的参数是谁传进来的？

---

## 三、怎么把一个类变成 Bean：@Component 与 @Bean

### 3.1 @Component（打在类上）

```java
@Component
public class BookQueryTool { ... }
```

- Spring 启动时扫描包（`@SpringBootApplication` 所在包及其子包），看到 `@Component` 就**自动创建对象放进容器**。
- 衍生注解（语义更清晰）：
  - `@Service` → 业务层
  - `@Repository` → 数据层
  - `@Controller` / `@RestController` → 控制层
  - 本质都是 `@Component`，扫描规则一样。

### 3.2 @Bean（打在方法上，放在配置类里）

```java
@Configuration
public class AppConfig {
    @Bean
    public AppInfo appInfo() {
        return new AppInfo("图书管理系统", "v1.0");
    }
}
```

- **什么时候用 @Bean**：
  - 这个类**不是自己写的**（第三方库，比如 RedisTemplate、RestTemplate），没法在类上加 @Component；
  - 创建过程需要复杂初始化逻辑（new 之前要做一堆准备）；
  - 需要根据条件决定创建哪个实例。
- **什么时候用 @Component**：自己写的业务类，直接让 Spring 扫描。

### 3.3 一句话对比

| 注解 | 放在哪 | 谁来调用 | 典型场景 |
|---|---|---|---|
| @Component 族 | 类上 | Spring 扫描自动创建 | 自己写的 Service/Mapper/工具 |
| @Bean | 方法上（配置类内） | Spring 调用方法拿返回值 | 第三方组件、复杂初始化、自定义组件 |

> 注意：@Bean 方法所在的类必须是 Spring 的 Bean（类上有 @Configuration 或 @Component），方法才会被处理。

### 3.4 自测

1. 自己写的类用哪个注解？第三方类用哪个？
2. @Bean 方法的方法名和返回值分别是什么角色？
3. @Service 和 @Component 有什么关系？

---

## 四、@Configuration 与 CGLIB 代理（你没看懂的重点）

### 4.1 先看现象

```java
@Configuration
public class AppConfig {
    @Bean
    public AppInfo appInfo() {
        System.out.println("执行 new AppInfo()");
        return new AppInfo();
    }

    @Bean
    public ApplicationRunner runner() {
        AppInfo a1 = appInfo();   // 配置类内部手动调用
        AppInfo a2 = appInfo();
        System.out.println(a1 == a2);   // 打印 true
        return args -> {};
    }
}
```

- 代码里写了两次 `appInfo()`，但 `new AppInfo()` **只执行 1 次**。
- `a1 == a2` 是 `true`，拿到**同一个单例对象**。

### 4.2 为什么？CGLIB 代理

`@Configuration` 会让 Spring 为这个类生成一个**代理子类**（运行时动态生成，源码里看不到）：

```text
你的源码：AppConfig
运行时：   AppConfig$$SpringCGLIB$$0  extends AppConfig（代理子类）
```

Spring 容器里放的是**代理子类的对象**，不是你写的 AppConfig 对象。
代理子类重写了 `appInfo()` 方法，逻辑是：

```text
public AppInfo appInfo() {
    // 先去容器查有没有 AppInfo 这个 Bean
    if (容器里有) {
        return 容器里那个;        // 不再执行 new
    }
    return super.appInfo();        // 容器里没有才真正执行你的方法
}
```

所以：
- 你调用 `appInfo()` → 实际走代理 → 查到容器已有单例 → 返回同一个对象。
- 效果：**配置类内部多次调用 @Bean 方法，拿到的都是同一个 Bean**，不会重复创建。

### 4.3 如果去掉 @Configuration，只留 @Component

```java
@Component
public class AppConfig {
    @Bean
    public AppInfo appInfo() { ... }

    @Bean
    public ApplicationRunner runner() {
        AppInfo a1 = appInfo();   // 普通 Java 方法调用
        AppInfo a2 = appInfo();
        System.out.println(a1 == a2);   // 打印 false！
        return args -> {};
    }
}
```

- `@Component` **不生成代理**。
- 内部调用 `appInfo()` 就是**普通方法调用**：写几次执行几次，每次都 new 新对象。
- `a1 == a2` 是 `false`。

### 4.4 划重点：两个概念别混淆

1. **Spring 调用 @Bean 方法（生成 Bean）**：不管 @Configuration 还是 @Component，都会调用，容器里都是单例（默认）。
2. **你自己在代码里手动调用 @Bean 方法**：只有 @Configuration 的代理会"拦截并返回容器单例"；没有代理就是普通方法，每次 new。

> 换句话说：**@Component + @Bean 也能注册单例 Bean；区别只在于"配置类内部互相调用 @Bean 方法时，是否被代理拦截"。**

### 4.5 proxyBeanMethods：开关代理

```java
@Configuration(proxyBeanMethods = true)   // 默认值：开启 CGLIB 代理
@Configuration(proxyBeanMethods = false)  // 关闭代理，行为类似 @Component
```

- `true`：内部调用被拦截，返回容器单例；代价是要生成代理类（启动稍慢）。
- `false`：不生成代理，启动更快；**约束：不要在类内部互相调用 @Bean 方法**，依赖全用方法参数注入。

### 4.6 最佳实践（记这个就够了）

```java
@Configuration(proxyBeanMethods = false)
public class AppConfig {

    @Bean
    public AppInfo appInfo() {
        return new AppInfo("图书管理系统", "v1.0");
    }

    // 不要写 appInfo() 去拿；直接方法参数注入，Spring 会从容器给单例
    @Bean
    public ApplicationRunner runner(AppInfo appInfo) {
        return args -> { ... };
    }
}
```

> 口诀：**依赖用参数注入，别在配置类里手动调 @Bean 方法，就不需要纠结代理。**

### 4.7 自测

1. @Configuration 和 @Component 的核心区别是什么？
2. 为什么 @Configuration 下内部调用 @Bean 方法能拿到单例？CGLIB 做了什么？
3. proxyBeanMethods = false 时要注意什么？
4. 面试官问"@Bean 方法能不能手动调用"，你怎么答？

---

## 五、@Bean 返回接口对象：ApplicationRunner 逐行拆解

### 5.1 原始代码（看不懂的那段）

```java
@Bean
public ApplicationRunner printAppInfoRunner(AppInfo appInfo) {
    return (ApplicationArguments args) -> {
        log.info("==================== 应用启动信息 ====================");
        log.info("应用名称：{}", appInfo.getAppName());
        log.info("应用版本：{}", appInfo.getVersion());
        log.info("======================================================");
    };
}
```

### 5.2 逐层拆解

**第 1 层：方法签名**
```java
public ApplicationRunner printAppInfoRunner(AppInfo appInfo)
```
- 返回值类型：`ApplicationRunner`（一个接口）。
- 参数 `AppInfo appInfo`：Spring 自动从容器注入（见 §2.3）。

**第 2 层：return 的是什么**
`ApplicationRunner` 是接口，接口不能 new。`return` 返回的是一个**实现了这个接口的对象**。
怎么得到一个接口的实现对象？两种写法（完全等价）。

**写法 A：匿名内部类（不简写，先看懂这个）**
```java
@Bean
public ApplicationRunner printAppInfoRunner(AppInfo appInfo) {
    ApplicationRunner runner = new ApplicationRunner() {
        @Override
        public void run(ApplicationArguments args) throws Exception {
            log.info("应用名称：{}", appInfo.getAppName());
        }
    };
    return runner;
}
```

**写法 B：Lambda（就是上面那段）**
```java
@Bean
public ApplicationRunner printAppInfoRunner(AppInfo appInfo) {
    return (ApplicationArguments args) -> {
        log.info("应用名称：{}", appInfo.getAppName());
    };
}
```

对应关系：

| Lambda 部分 | 对应匿名内部类内容 |
|---|---|
| `(ApplicationArguments args)` | `public void run(ApplicationArguments args)` 的参数 |
| `->` | 分割参数与方法体 |
| `{ ... }` | `run()` 方法体 |

### 5.3 最容易误解的点：return 时大括号里的代码**不会立刻执行**

```text
程序执行到 return (args) -> { log... }：
  只把"接口实现对象"交给 Spring 容器，log 不打印！

Spring 启动流程全部完成后：
  Spring 自动调用这个对象的 run() 方法 → 才执行 log
```

> 类比：你写了一个闹钟工厂方法，`return 闹钟` 只是把闹钟交给 Spring。**闹钟不会立刻响**。等时间到（容器启动完成），Spring 按一下闹钟的"响铃"方法（run），才响。

### 5.4 ApplicationRunner 是什么

```java
public interface ApplicationRunner {
    void run(ApplicationArguments args) throws Exception;
}
```

- Spring Boot **容器全部初始化完成、项目启动成功之后**，自动执行所有 `ApplicationRunner` Bean 的 `run()`。
- 适合：打印启动横幅、预加载数据、检查环境。
- 与 `CommandLineRunner` 几乎一样，区别只在参数：
  - `ApplicationRunner`：参数被封装成 `ApplicationArguments` 对象（方便取选项参数）。
  - `CommandLineRunner`：直接拿原始 `String[] args`。
  - 两个都执行，没有规定先后（通常按声明顺序）。

### 5.5 自测

1. 接口能 new 吗？return 一个接口类型时，返回的到底是什么？
2. Lambda 和匿名内部类是什么关系？
3. `return (args) -> { log }` 这行执行时，log 会打印吗？什么时候打印？
4. ApplicationRunner 和 CommandLineRunner 的区别？

---

## 六、Lambda 与匿名内部类（补基础）

### 6.1 函数式接口

只有一个抽象方法的接口，叫**函数式接口**，可以用 Lambda 简写。
通常标 `@FunctionalInterface`（不是必须，但推荐）。

```java
@FunctionalInterface
public interface ApplicationRunner {
    void run(ApplicationArguments args) throws Exception;
}
```

### 6.2 Lambda 语法

```text
(参数列表) -> { 方法体 }
```

- 参数类型可省略：`(ApplicationArguments args)` → `(args)`（类型由接口推断）。
- 方法体只有一行可去掉大括号和 return。
- 变量捕获：Lambda 可以使用外层方法/类的变量（实际上要求变量是 final 或事实 final）。

### 6.3 对照表

```java
// 完整匿名内部类
Runnable r1 = new Runnable() {
    @Override
    public void run() {
        System.out.println("hi");
    }
};

// Lambda
Runnable r2 = () -> System.out.println("hi");
```

### 6.4 自测

1. 什么样的接口能用 Lambda？
2. 把 `(ApplicationArguments args) -> { log.info(...); }` 改写成匿名内部类。
3. Lambda 里能用外层变量吗？有什么限制？

---

## 七、Spring Boot 启动流程（把这些串起来）

```text
main() 方法
   ↓ SpringApplication.run(...)
   ↓ 创建 Spring 容器（IOC 容器）
   ↓ 自动配置（AutoConfiguration）生效
   ↓ 组件扫描（扫描 @Component/@Service/@Controller... 放进容器）
   ↓ 处理 @Configuration + @Bean 方法（把返回对象放进容器）
   ↓ 依赖注入（给 Bean 的方法参数/构造器/字段注入依赖）
   ↓ 容器初始化完成
   ↓ 执行 ApplicationRunner / CommandLineRunner 的 run()
   ↓ 项目对外提供服务（8080 端口可访问）
```

> 我们的 AppConfig 例子对应流程：
> 1. Spring 扫描到 `@Configuration AppConfig`，创建代理子类对象；
> 2. 调用 `appInfo()` 方法，把 AppInfo 放进容器；
> 3. 调用 `printAppInfoRunner(AppInfo appInfo)`，参数注入容器里的 AppInfo，把返回的 runner 放进容器；
> 4. 启动完成，Spring 自动调用 runner.run()，打印日志。

### 自测

1. 按顺序说出 Spring Boot 启动的主要步骤。
2. ApplicationRunner 在哪个步骤之后执行？
3. 如果启动中途报错，runner 会执行吗？（答案：不会，容器都没启动成功。）

---

## 八、本知识点对应的常见面试追问

1. IOC 和 DI 的区别与关系？
2. 构造器注入和字段注入的区别？为什么推荐构造器？
3. @Component 和 @Bean 的区别？什么时候用哪个？
4. @Configuration 和 @Component 的区别？CGLIB 代理做了什么？
5. proxyBeanMethods 的作用？什么时候关掉？
6. Spring Boot 启动完成后想执行一段代码，有哪些方式？（ApplicationRunner / CommandLineRunner / @PostConstruct / ApplicationReadyEvent）
7. Lambda 与匿名内部类等价吗？有什么细微差别？（this 指向、变量捕获、是否生成额外类）
